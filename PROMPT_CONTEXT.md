# PROMPT_CONTEXT — AEGIS Session Contract

Before changing AEGIS, read this file and the current canonical authority documents.

## Non-negotiable rules

1. Read repository state before generating implementation logic.
2. Current target build is the controlling runtime context.
3. Never assume undocumented AoE2DE ABI semantics.
4. Never use stock Promisory as an accidental runtime dependency.
5. Treat the complete stock HD/Promisory substrate as reconstruction evidence until a capability is independently rehosted under AEGIS ownership.
6. Never overwrite another module's goal/SN/timer/flag namespace.
7. Never promote a numeric channel because it merely appears unused.
8. Distinguish engine semantics, validator behavior, and AEGIS design intent.
9. Distinguish command issued, engine-observed completion, world postcondition, and strategic success.
10. Preserve target-build provenance on every material ABI claim.
11. Distinguish OBSERVED, CORRELATED, INFERRED, IMPLEMENTED, STATIC-QUALIFIED, RUNTIME-QUALIFIED, and STRESS-QUALIFIED claims.
12. Do not call an implementation final before its required QC gates pass.
13. Do not start runtime experiments for a subsystem that has not yet been deconstructed and given an explicit qualification contract.
14. Do not create disposable implementation candidates when the task is to implement a production subsystem.
15. Preserve negative results and contradictions; do not silently erase them.

## Current phase directive

**TOTAL-SYSTEM DECONSTRUCTION.**

The immediate job is to deconstruct the entire stock `AI (HD version).per` and the original Promisory load/subsystem graph: constants, declarations, rule topology, state channels, timers, object/search state, production/build/research flows, worker/economic state machines, scouting, threats, military, trade, navy, cooperation, and cross-system feedback.

The next implementation phase begins only after the reconstruction is coherent enough to define AEGIS ownership and ABI requirements.

## Registry discipline

Use:

- `docs/syntax/AI_SYNTAX_RULES.md`
- `docs/stock/`
- `state/`
- `evidence/EVIDENCE_LEDGER.csv`
- `harness/`

as control-plane artifacts. They are authoritative only within their stated evidence class and never override stronger target-build evidence.

## Source boundary

The final bot is AEGIS-owned. Historical Promisory mechanisms may be reproduced as independently implemented behavior where justified, but the final runtime must not rely on the original stock Promisory tree merely because it exists on the machine.
