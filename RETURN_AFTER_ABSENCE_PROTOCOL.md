# AEGIS — Return After Absence Protocol

Purpose: allow the project to resume after months away without depending on conversational memory.

## First 10 minutes

1. Open the repository at the latest commit on the active research branch.
2. Read `README.md`.
3. Read `PROJECT_MATERIALS.md`.
4. Read `RESEARCH_INDEX.md`.
5. Read `07_EXPERIMENTS/AEGIS_LAYER1_LAB/PROFESSIONAL_HANDOFF_2026-09-03.md`.
6. Confirm the formal status is **Layer 1: 89%**.
7. Do not infer progress from file count. Read the QC table and causal frontier.

## Current checkpoint

Research branch at handoff:
`lab/aoe2de-adapter-2026-09-03`

Handoff commit containing the professional state:
`24cd5289e9b83df35c672b21c0c3c3d50d47224f`

Preceding scheduler-recovery checkpoint:
`9fb43d9f6e40bd7e13960b4c4709acabd1af501b`

Exact controlled executable:
`AoE2DE_s.exe`

Build:
`101.103.48987.0`

SHA-256:
`6378CA6F1FBD2F230B5B7F2CD048198331848AF70F44B5CD13CEB89420A321A4`

## Do not lose these distinctions

- Native executable evidence is build-scoped.
- Historical public source is comparative evidence.
- Replay evidence is observational.
- Parser behavior is tooling evidence.
- Validator behavior is not runtime proof.
- Strings identify semantic neighborhoods; they do not prove call edges.
- `.pdata` identifies executable ranges; it does not identify semantics.
- A generated experiment is not a successful causal experiment unless its observations discriminate competing hypotheses.
- Internal test-harness strings in the executable do not by themselves establish authorization to activate undocumented interfaces.
- XS is not part of the final pure-`.per` ByzBot architecture.

## What was completed before the absence

- Stock AI corpus and reachable `.per/.per2/.xs` composition reconstructed.
- DAT/unit-line evidence incorporated.
- Replay parsing workflow established.
- AoE2ScenarioParser qualified in an isolated environment.
- Automated Layer 1 experiment harness bootstrap created.
- Native runtime controller/security gates established.
- Exact build fingerprint established.
- Native startup/background runtime operation established as infrastructure evidence.
- Native `.per` loader / AIExpert archaeological boundary established.
- Native scenario AI embedding boundary established.
- Internal test-harness archaeology completed but activation deliberately shelved.
- Scenario Editor automation attempted and shelved as a primary causal route.
- Native scheduler recovery pass performed; no current RunList/loadRules/scheduler function promoted.
- Historical `aoe2-ai-module` RunList lead tested against the current binary and rejected as a current signature because it matched zero times.

## Current unsolved causal chain

The major missing causal path is:

`AI file → parsed rules → rule list → eligibility → ordering → selected rule → action dispatch → UnitAI mutation → execution → failure/recovery`

Persistent facts cut across this chain:

`fact source → evaluation → storage → invalidation/refresh → consumer`

## Next task

### P0-B Native Scheduler Recovery

Start from the exact current executable.

1. Verify the SHA-256.
2. Partition executable code using `.pdata`.
3. Disassemble individual runtime-function intervals, not the whole `.text` as one stream.
4. Build reverse references from `.rdata/.data` to `.pdata` function starts.
5. Use independently supported AIExpert field-layout evidence to rank candidate functions.
6. Locate constructor/loadRules neighborhoods.
7. Follow callers/callees into rule-list execution.
8. Recover eligibility and ordering operations.
9. Design a minimal discriminating runtime calibration only after static convergence.
10. Record rejection reasons as carefully as successful candidates.

## If the game has updated

Do **not** continue using old native conclusions as though they were current.

1. Hash the executable.
2. Record build/version/date.
3. Compare against the handoff hash.
4. If different, create a new build-revision boundary artifact.
5. Re-run only the necessary archaeological anchors first.
6. Treat old native addresses/signatures as historical until revalidated.
7. Preserve the old evidence; do not rewrite it.

## If the machine/environment has changed

Re-establish:
- executable path;
- isolated lab root;
- Python version and parser environment lock;
- exact parser source digest;
- repository branch;
- authorized remote device, if runtime testing is resumed;
- output/artifact containment.

Environment drift is not a research result.

## Definition of 'back where we were'

You are back at the same project state when you can reproduce:
1. the 89% Layer 1 QC classification;
2. the exact build identity or an explicitly recorded new build boundary;
3. the evidence hierarchy;
4. the completed and shelved routes;
5. the current P0/P1 frontier;
6. the rejection of the historical RunList signature as a current address claim;
7. the next-pass procedure without relying on this conversation.

This protocol intentionally favors reproducibility over conversational continuity.
