# AEGIS-BYZ — P0 AI(HD)+PROMISORY PASS

**Target:** AoE2DE 101.103.48987.0 / BuildID 24094652
**Persona:** Personification of AI(HD)+Promisory — Behavioral Adversary
**Verdict:** Preserve behavioral contracts, not historical implementation.

## Mission
The stock AI is adversarial behavioral memory. It identifies continuity, edge cases, hidden coupling, and recovery that a clean rewrite may accidentally remove. It may challenge architecture but may not demand cargo-cult code duplication.

## Contracts to preserve

### Workers
Worker role is operational state. Assignment depends on eligibility, order/action, cargo, target, source, spatial relationship, and task load. Idle is not a sufficient state model.

### Economy
Stock resource acquisition follows target worker vector → actual census → deficit → eligible workers → source candidates → dropsite/distance filtering → task-load filtering → resource-specific filters → command → observation → recovery. Static percentages are policy inputs, not the physical controller.

### Source failure
Preserve distinctions among source absence, unusable source, overload, poor service distance, missing infrastructure, invalid worker/task state, and exhausted food paths.

### Construction
Building count does not prove completion. Preserve placement, builder assignment, foundation/pending state, progress, completion, cleanup, and recovery.

### Production
Production is distributed arbitration involving goals, suppression, affordability, escrow, endpoint availability, pending state, and rule ordering. Replace fragile topology while preserving the behavioral purpose.

### Escrow
Engine escrow is real substrate. AEGIS logical reservation must remain conceptually distinct unless explicitly mapped and qualified.

### Scouting
Scouting is information acquisition: target selection, routing, safety, discovery, retreat, and recovery all matter.

### Threats
Threat-blocked is not the same as task-invalid. Threat behavior is distributed; do not invent a universal evacuation manager without evidence.

### Food
Food is a portfolio of herdables, forage, boar, deer/hunt, farms, fishing, and map-specific sources. Build lifecycle and recovery before optimization.

## Hidden coupling warnings
Do not remove pending-state checks, source-specific task-load thresholds, builder target validation, dropsite relationships, bounded recovery, spending suppression, generation/freshness boundaries, production endpoint selection, threat-aware filtering, worker cargo/task-state checks, or scout recovery without an explicit replacement contract.

## Migration law
Every stock mechanism becomes:

```text
STOCK CODE
→ OBSERVED BEHAVIOR
→ FAILURE PREVENTED
→ REQUIRED CONTRACT
→ AEGIS OWNER
→ AEGIS STATE
→ AEGIS RECOVERY
```

If the implementation is obsolete, discard the implementation but preserve the demonstrated contract.

## Attack on current AEGIS
The current cognitive chain has useful generation fencing and separates execution from verification. Preserve those boundaries. Operations remains the weak substrate point because its static worker percentages do not reproduce stock economic continuity. Belief correctly consumes World Model state without issuing commands; preserve that ownership boundary.

## Final verdict
AI(HD)+Promisory is a regression oracle, not the blueprint. The first vertical slice is approved only if it is adversarially tested against worker continuity, source failure, construction continuity, production arbitration, pending state, threat interaction, and recovery.