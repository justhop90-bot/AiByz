# AEGIS-BYZ — P0 ARCHAEOLOGIST PASS
## Structural Reconciliation and ABI Gate Review — 2026-09-08

**Target:** AoE2DE 101.103.48987.0 / BuildID 24094652
**Persona:** The Archaeologist — Machine Truth
**Verdict:** NO BLIND SUBSTRATE CODING; existing AEGIS-Lite evidence must be reconciled before promotion.

## 1. Established evidence

The repository contains a machine-tested AEGIS-Lite chain from World Model through Commitment. The handoff records successful sequential startup/execution observations while explicitly warning that startup success is not semantic qualification.

The tested chain is:

```text
WORLD MODEL → BELIEF → SITUATION → OBJECTIVES → PLANNING → DECISION → COMMITMENT
```

The published P2 Carpenter owns native sensor expression while the Architect owns World Model meaning, generation and qualification. That ownership split is architecturally sound in principle.

## 2. Structural gate

Issue #46 remains the controlling reconciliation gate. Its recorded machine audit found duplicate World Model declarations in the root and P2 Carpenter artifact and separately identified two Carpenter artifacts. The required action is to preserve the exact machine-tested source, choose one World Model owner, choose one Carpenter owner, reconcile the closure, and rerun generation experiments.

## 3. Critical ruling

The old AEGIS-Lite candidate is historical/test evidence, not automatically the package to install or merge. The current live AEGIS runtime is a newer modular architecture. Therefore Issue #46 must NOT be solved by copying the old P2 package into the live runtime.

Instead:

```text
machine-tested artifact
→ recover tested contracts/evidence
→ reconcile ownership
→ preserve useful semantics
→ qualify current architecture
```

## 4. Generation evidence

A prepared generation propagation experiment exists and explicitly remains runtime-UNKNOWN. It separates N→N+1 propagation, stale-generation rejection, and invalid-observation cases.

Therefore generation fencing is an architectural contract, but universal same-pass visibility remains unqualified.

## 5. Actuator evidence

A separate spearman-line training actuator candidate exists and remains unloaded. It correctly distinguishes ISSUED from accepted, created, deployed, and effective. It is reusable as a pattern, not runtime permission.

## 6. Archaeologist ruling

1. Preserve P2 as historical machine-tested evidence.
2. Do not merge its duplicate World Model declarations into live Foundation.
3. Define one current World Model owner and one Carpenter owner.
4. Build probes against the current package shape.
5. Qualify only ABI assumptions required by the civilian slice.
6. Preserve ISSUED versus OBSERVED versus VERIFIED boundaries.
7. Bind every runtime result to source/package/executable hashes.

## 7. First-slice ABI minimum

The civilian vertical slice requires evidence for:

- goal mutation visibility;
- generation publication/consumer visibility;
- native train command issuance;
- population observation after training;
- pending-object observation where used;
- housing/build command issuance;
- foundation/completion observation;
- worker search and target mutation;
- task command issuance;
- worker action/order/cargo/source observation;
- timer/re-entry behavior needed for safe reconciliation.

Jump semantics should be isolated as a separate experiment rather than introduced merely because stock uses them heavily.

## 8. Required state boundary

Until same-pass behavior is qualified, architecture must rely on observable boundaries:

```text
REQUESTED
→ AUTHORIZED
→ COMMAND_ISSUED
→ ENGINE_OBSERVED
→ PRODUCTIVE / COMPLETE
→ VERIFIED
```

## 9. Status

**Structural reconciliation:** OPEN / BLOCKING
**Generation propagation:** UNKNOWN
**Stale rejection:** UNKNOWN
**Command visibility:** UNKNOWN
**Rule-pass semantics:** UNKNOWN
**Current architecture:** suitable for targeted continuation after ownership audit
**Production substrate:** NOT YET PROMOTED

The Archaeologist releases the project to the Byzantine Architect with one binding condition: unresolved interpreter behavior stays behind observable state boundaries.
