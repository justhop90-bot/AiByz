# AiByz — Three-Pass Quality Control

## Pass 1 — Repository Integrity and Evidence Architecture

**Date:** 2026-09-03  
**Scope:** public repository `justhop90-bot/AiByz`, default branch state at the start of the three-pass review  
**Disposition:** PASS with corrective actions

## 1. Objective

Determine whether the repository remains a trustworthy institutional record after the latest Layer 1 investigation, whether its navigation and authority boundaries are coherent, and whether the newly established evidence can be incorporated without contaminating the evidence hierarchy.

## 2. Baseline examined

The repository identifies itself as the long-term research and engineering record for AEGIS, with Layer 1 explicitly closed for handoff at 89% rather than certified complete. The README directs returning engineers to the final Layer 1 handoff, evidence matrix, predictive standard, completion control, native archaeology records, and atomic knowledge ledgers.

The research index independently preserves the same recovery sequence and explicitly distinguishes observation, interpretation, inference, hypothesis, and architecture.

## 3. Integrity findings

### 3.1 Authority hierarchy — PASS

The repository correctly prevents historical AI source, replay observations, validator behavior, and symbolic/native vocabulary from silently becoming current-runtime truth. The controlled executable identity is explicitly scoped to the exact build under examination.

### 3.2 Layer boundary — PASS

The repository does not prematurely promote Layer 2/3/4 implementation into the Layer 1 machine model. The current architecture is explicitly described as an engineering consequence of evidence rather than a claim about literal shipped C++ class structure.

### 3.3 Native evidence discipline — PASS

The `.pdata` coordinate system, CodeView/PDB identity, direct-reference negative results, and rejected metadata-pointer candidate are represented as bounded evidence rather than as semantic certainty.

### 3.4 Recovery/navigation — PASS

The front door is unusually strong: README → research index → final handoff → completion/predictive standards → evidence matrix → archaeology/QC → atomic ledgers.

### 3.5 Quantitative correction — REQUIRED

The repository's current Layer 1 handoff is correct to retain **89%**. A prior 7,715 reachable-rule figure is not authoritative and must not be repeated. The latest recursive stock-AI analysis establishes a qualified count of **7,831 syntactically reachable `defrule` definitions across 28 reachable `.per/.per2/.xs` files when conditional branches are conservatively included**. This is a corpus-coverage statistic, not a per-game execution count.

### 3.6 New-material representation — REQUIRED

The newly inspected `gamedata_x2`, `dat`, `xs`, stock-AI archives, and exact executable must enter the institutional record through provenance/manifests and evidence notes, not by indiscriminate binary redistribution. Their findings belong in the public research layer only to the extent that publication rights and evidence boundaries permit.

## 4. Pass-1 corrections

1. Retain Layer 1 at 89%; do not inflate the score from corroboration alone.
2. Retire the 7,715 rule-count claim.
3. Adopt the qualified 7,831 syntactically reachable definition count with its exact qualification.
4. Preserve the exact controlled executable SHA-256 and build scope.
5. Record new data/AI/XS material as evidence inputs, not as replacement authority.
6. Keep failed experiments and false-positive archaeology as negative knowledge.
7. Keep native addresses/build-specific findings explicitly non-portable.

## 5. Pass-1 conclusion

**PASS.** The repository's conceptual integrity is sound. The corrective work is primarily synchronization: bring the newest corpus measurement, source inventories, external corroboration, and current native findings into one dated QC layer without weakening the evidence hierarchy.
